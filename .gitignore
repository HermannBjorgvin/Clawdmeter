# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/

# OS
.DS_Store

# Claude Code
.claude/

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# Mac daemon venv
daemon/.venv/

# Windows daemon launcher rendered from start-daemon.cmd.template by install-windows.ps1
# (absolute paths are machine-specific; the template is the source of truth).
daemon/start-daemon.cmd

# Python bytecode cache
__pycache__/
*.pyc
